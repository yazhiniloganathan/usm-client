/* global define */
(function() {
    'use strict';
    define(['lodash'], function(_) {

        var ClusterController = function($scope) {

        };
        return ['$scope', ClusterController];
    });
})();
